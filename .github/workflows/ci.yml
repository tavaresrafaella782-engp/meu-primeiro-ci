name: CI Pipeline

on:
  push:
   branches: [main]
  pull_request:
   branches: [main]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
     - name: Baixarcódigo
     - uses: actions/checkout@v4

     - name: Instalar dependências
       run: npm install

     -name: Rodar testes
      run: npm test
